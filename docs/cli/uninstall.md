---
summary: "CLI reference for `opencog uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `opencog uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
opencog uninstall
opencog uninstall --all --yes
opencog uninstall --dry-run
```
